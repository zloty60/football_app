import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#48a999",
      main: "#00796b",
      dark: "#004c40",
      contrastText: "#fff"
    }
  }
});

export default theme;
